<h3>確認</h3>

<%current_cart.cart_items.each do |ci|%>
  <%= ci.product.name%>
  <%= ci.product.price%>
<%end%>

<%=link_to "購入する", orders_path(order: {total: params[:cart_total]}), method: :post, class:"btn btn-primary"%>
